{
  "name": "question-bank-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler check",
    "db:local": "wrangler d1 migrations apply question-bank-db --local",
    "db:remote": "wrangler d1 migrations apply question-bank-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.52.0"
  }
}
